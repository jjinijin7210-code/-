// ============================================================
// 인스타그램 댓글 자동 응답 (CS 자동 응답) - 2026-07-26 이전엔 진희님 컴퓨터에서 Playwright로
// 실제 로그인 세션에 들어가 게시물 여러 개를 순회하며 댓글을 스캔하고, 트리거 키워드가
// 매칭되면 "답글 달기"→입력→"게시" 버튼까지 전부 자동으로 클릭해서 실제로 게시했음
// (스캔뿐 아니라 답글 전송의 마지막 클릭까지 스크립트가 수행 - 사람 개입 없음).
//
// 2026-07-26: 진희님 실제 틱톡 계정이 영구정지됨 - 브라우저 자동화로 로그인 세션을 스크립트가
// 조작하는 방식 자체가 원인으로 유력해서, 같은 패턴을 쓰는 인스타그램 자동화(게시 자동 입력,
// routes/instagram.js)를 진희님 지시로 즉시 중단함. 이 댓글 자동응답 기능은 그것보다 더
// 위험함(반복적으로 여러 게시물을 순회 + 답글 게시까지 완전 자동) - 확인 없이 먼저 중단함.
// CLAUDE.md의 SNS 자동화 5원칙 1번("비공식 API·브라우저 자동화 절대 금지, 공식 Meta Graph
// API로만 구현")과 정확히 이 사례가 위반 사례였음 - 다시 켤 땐 반드시 공식 Graph API
// (댓글 조회·답글 API)로 새로 만들 것, 이 브라우저 자동화 방식으로 되살리면 안 됨. 원래
// 구현은 git 이력에 남아있음.
// ============================================================

import { Router } from 'express'

const router = Router()

router.post('/instagram/scan-comments', async (req, res) => {
  res.status(403).json({
    error: '계정 보호를 위해 인스타그램 댓글 자동 스캔을 껐어요 (틱톡 계정 영구정지 후 안전 조치). 댓글 확인·답글은 인스타그램 앱/웹에서 직접 해주세요.',
  })
})

router.post('/instagram/reply-comment', async (req, res) => {
  res.status(403).json({
    error: '계정 보호를 위해 인스타그램 자동 답글을 껐어요 (틱톡 계정 영구정지 후 안전 조치). 댓글 확인·답글은 인스타그램 앱/웹에서 직접 해주세요.',
  })
})

export default router
